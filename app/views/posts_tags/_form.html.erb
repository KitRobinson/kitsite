<%= form_for(@posts_tag) do |f| %>
  <% if @posts_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@posts_tag.errors.count, "error") %> prohibited this posts_tag from being saved:</h2>

      <ul>
      <% @posts_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
